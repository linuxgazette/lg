server_host = 127.0.0.1
server_port = 389
search_base = lookupName=aliases,cn=postfix,cn=ganesha,ou=server,ou=bund,dc=gruene,dc=at
timeout     = 30
bind        = yes
bind_dn     = cn=postfix,ou=system,ou=accounts,dc=gruene,dc=at
bind_pw     = squannyxecticess
version     = 3

start_tls        = no
tls_ca_cert_file = /etc/ldap/openldap/collected_cas.pem

result_attribute = lookupValue
